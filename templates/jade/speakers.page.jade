mixin speakerInfo(speaker)
    h3= speaker.name
    if speaker.website
        .speaker-website: a(href=speaker.website.url, target="_blank")= speaker.website.name
    p= speaker.intro

extends layout.jade

block content
    h1 Speakers 講者
    .speaker-list.pure-g
        each speaker in speakers
            .pure-u-1.pure-u-md-1-2.pure-u-lg-1-3
                .hex.hex-o.hex-o-s1: .hex-o-h: .hex.hex-s1
                    .hex-img.hex-img-blurred: img(src=speaker.photoUrl)
                    .hex-img.hex-img-origin: img(src=speaker.photoUrl)
                    .hex-content.speaker-intro
                        +speakerInfo(speaker)
                .hex-content-sm
                    +speakerInfo(speaker)
